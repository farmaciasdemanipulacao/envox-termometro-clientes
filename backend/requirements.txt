# === CORE FRAMEWORK ===
fastapi==0.115.5
uvicorn[standard]==0.32.1

# === BANCO DE DADOS ===
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# === VALIDAÇÃO E CONFIG ===
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
email-validator==2.2.0

# === AUTENTICAÇÃO ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.18

# === JOBS AGENDADOS ===
apscheduler==3.10.4

# === LOGGING ESTRUTURADO ===
structlog==24.4.0

# === ANÁLISE DE TEXTO (leve) ===
# Usamos regras + keywords, sem modelos pesados no MVP
# spaCy pode ser adicionado na Fase 2

# === HTTP CLIENT (para conectores futuros) ===
httpx==0.28.1

# === UTILITÁRIOS ===
python-dateutil==2.9.0.post0
pytz==2024.2

# === IA: WHISPER (transcrição de áudio) + LLM ===
openai==1.56.0

# === TESTES ===
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
